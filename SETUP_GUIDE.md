# 🚀 РУКОВОДСТВО ПО ЗАПУСКУ FitZen

Добро пожаловать! Это полное руководство по запуску вашего проекта FitZen.

## ✅ Что уже готово

Проект **полностью разработан** и включает:

- ✅ **Backend API** - Все endpoints для Users, Auth, Workouts, Stretching, Meditation, Activities, Progress
- ✅ **Аутентификация** - JWT + bcrypt, защищенные маршруты
- ✅ **База данных** - Полная Prisma схема с 9 моделями
- ✅ **Frontend UI** - Все основные страницы (Dashboard, Workouts, Stretching, Meditation, Progress)
- ✅ **UI компоненты** - Button, Card, Input, Spinner, Badge
- ✅ **Тестовый контент** - 10 тренировок, 5 растяжек, 5 медитаций
- ✅ **Адаптивный дизайн** - Работает на всех устройствах
- ✅ **Dark mode** - Поддержка темной темы

## 🔧 ШАГ 1: Настройка Базы Данных

### Вариант А: Локальная PostgreSQL (для тестирования)

1. **Установите PostgreSQL** (если ещё не установлен):
   ```bash
   # macOS
   brew install postgresql@15
   brew services start postgresql@15

   # Linux (Ubuntu/Debian)
   sudo apt install postgresql postgresql-contrib
   sudo systemctl start postgresql

   # Windows
   # Скачайте установщик с postgresql.org
   ```

2. **Создайте базу данных**:
   ```bash
   psql postgres
   CREATE DATABASE fitzen;
   \q
   ```

3. **Файл `.env` уже создан** с локальной строкой подключения:
   ```
   DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fitzen?schema=public"
   ```

### Вариант Б: Облачная PostgreSQL (для production)

Рекомендуемые сервисы (все с бесплатными планами):

1. **Neon** (https://neon.tech) - Быстрый, современный
2. **Supabase** (https://supabase.com) - Много возможностей
3. **Railway** (https://railway.app) - Простой в использовании

**Инструкция:**
1. Создайте аккаунт на любом из сервисов
2. Создайте новую PostgreSQL базу данных
3. Скопируйте строку подключения (Connection String)
4. Обновите `.env` файл:
   ```env
   DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
   ```

## 🗄️ ШАГ 2: Инициализация Базы Данных

Выполните команды по порядку:

```bash
# 1. Установите зависимости (если ещё не установлены)
npm install

# 2. Генерация Prisma Client
npm run prisma:generate

# 3. Создание таблиц в БД
npm run prisma:migrate

# При запросе имени миграции введите: "init"

# 4. Заполнение тестовыми данными
npm run prisma:seed
```

**Ожидаемый результат:**
```
🌱 Seeding database...
✅ Created 10 workouts
✅ Created 5 stretching sessions
✅ Created 5 meditations
🎉 Seeding completed successfully!
```

## 🚀 ШАГ 3: Запуск Приложения

```bash
npm run dev
```

Откройте http://localhost:3000

## 👤 ШАГ 4: Первый Вход

1. Откройте http://localhost:3000
2. Нажмите "Зарегистрироваться"
3. Создайте аккаунт:
   - **Имя**: Ваше имя
   - **Email**: test@example.com
   - **Пароль**: minimum 8 символов

4. После регистрации вы автоматически войдете в Dashboard!

## 🎯 ШАГ 5: Тестирование Функционала

### Dashboard
- Посмотрите статистику (пока пустую)
- Проверьте быстрые действия

### Тренировки
1. Перейдите в раздел "Тренировки"
2. Увидите 10 тестовых тренировок
3. Попробуйте фильтры (по типу и уровню)
4. Нажмите на любую тренировку

### Растяжка
- 5 готовых сессий растяжки
- С видео-ссылками на YouTube

### Медитация
- 5 медитаций с аудио
- Разные категории

### Прогресс
1. Перейдите в "Прогресс"
2. Добавьте первую запись:
   - Вес: 75.0 кг
   - Заметки: "Начало пути!"
3. Нажмите "Сохранить"

## 🌐 ШАГ 6: Деплой на Production (опционально)

### Подготовка

1. **Создайте облачную БД** (см. Шаг 1, Вариант Б)

2. **Создайте аккаунт на Vercel**:
   - Перейдите на https://vercel.com
   - Зарегистрируйтесь через GitHub

### Деплой

1. **Подключите репозиторий**:
   - В Vercel нажмите "Add New Project"
   - Выберите этот репозиторий
   - Нажмите "Import"

2. **Настройте переменные окружения**:
   ```
   DATABASE_URL=<ваша облачная строка подключения>
   JWT_SECRET=<сгенерируйте случайную строку 32+ символов>
   JWT_REFRESH_SECRET=<сгенерируйте другую случайную строку>
   NEXT_PUBLIC_APP_URL=https://ваш-домен.vercel.app
   ```

3. **Настройте Build Command**:
   - Build Command: `prisma generate && next build`
   - Install Command: `npm install`

4. **Deploy!**

5. **После деплоя выполните миграции**:
   - В терминале Vercel или локально:
   ```bash
   DATABASE_URL="ваша_облачная_строка" npx prisma migrate deploy
   DATABASE_URL="ваша_облачная_строка" npx prisma db seed
   ```

## 🔧 Возможные Проблемы

### Ошибка подключения к БД
```
Error: P1001: Can't reach database server
```
**Решение**: Проверьте DATABASE_URL в `.env` файле

### Prisma не находит схему
```
Error: Schema file not found
```
**Решение**:
```bash
npx prisma generate
```

### JWT ошибки
```
Error: secret or public key must be provided
```
**Решение**: Убедитесь что в `.env` есть `JWT_SECRET` и `JWT_REFRESH_SECRET`

### Ошибка при seed
```
Error: Table 'workouts' doesn't exist
```
**Решение**: Сначала выполните миграцию:
```bash
npm run prisma:migrate
```

## 📚 Полезные Команды

```bash
# Разработка
npm run dev              # Запуск dev сервера
npm run build            # Сборка для production
npm run start            # Запуск production сервера

# База данных
npm run prisma:studio    # Открыть Prisma Studio (GUI для БД)
npm run prisma:generate  # Генерация Prisma Client
npm run prisma:migrate   # Создать и применить миграцию
npm run prisma:seed      # Заполнить БД тестовыми данными

# Prisma Studio
npm run prisma:studio    # Удобный GUI для просмотра и редактирования данных
```

## 🎨 Кастомизация

### Изменение цветовой схемы
Откройте `tailwind.config.ts` и измените:
```typescript
colors: {
  primary: { ... },    // Цвет для тренировок
  secondary: { ... },  // Цвет для медитаций
  success: { ... },    // Цвет для прогресса
}
```

### Замена тестового контента
1. Откройте Prisma Studio: `npm run prisma:studio`
2. Перейдите в таблицу `Workout` / `StretchingSession` / `Meditation`
3. Редактируйте записи напрямую
4. Или измените `prisma/seed.ts` и запустите заново

### Добавление своих видео/аудио
Просто замените `videoUrl` и `audioUrl` на свои ссылки (YouTube, Vimeo, etc.)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте `.env` файл
2. Убедитесь что БД запущена
3. Посмотрите логи в терминале
4. Проверьте `prisma/schema.prisma` на ошибки

## 🎉 Готово!

Ваш проект FitZen полностью готов к использованию!

**Что дальше?**
- Добавьте своих тренировок
- Пригласите друзей
- Отслеживайте прогресс
- Наслаждайтесь!

---

**Версия**: 1.0.0
**Дата**: Ноябрь 2025
**Разработчик**: Claude + Ваша команда 🚀
